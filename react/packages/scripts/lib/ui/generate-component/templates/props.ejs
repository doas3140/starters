import * as React from 'react'

export type <%= component %>Props = {
  }

export const <%= component %>DefaultProps: <%= component %>Props = {
  }  

export const <%= component %> = (props: <%= component %>Props = <%= component %>DefaultProps) => {
    return (
        <>
        </>
    )
}
